# ------------------------------------------------------------------------------
# Copyright (C) 2016-2017 DELL EMC Corporation. All Rights Reserved.

# This software contains the intellectual property of DELL EMC Corporation
# or is licensed to DELL EMC Corporation from third parties.  Use of this
# software and the intellectual property contained therein is expressly
# limited to the terms and conditions of the License Agreement under which
# it is provided by or on behalf of DELL  EMC.

# ------------------------------------------------------------------------------

---
-
    name: '{{ step|default(None) }} - SQL Server restore DB'
    hosts: '{{ vms }}'
    gather_facts: no
    any_errors_fatal: True
    tasks:
        -
            name: '{{ step|default(None) }} - Restore SQL Server DB from backup'
            tags:
                - '{{ step|default(None) }} - Restore SQL Server DB from backup'
            delegate_to: localhost
            ehc_script_agent:
                package_name: sql
                module_name: restoreDBBackup
                method_name: restoreSQLDBBackup
                method_params:
                    vCenterDict: '{{ vcenter }}'
                    sqlDict:
                        host: '{{ host_address.sql_server.fqdn }}'
                        user: 'sa'
                        password: '{{ sql_server_details.sa_password }}'
                        dbName: '{{ dbName }}'
                        vmname: '{{ host_address.sql_server.vmname }}'
                        vmusername: '{{ windows.default_username }}'
                        vmpassword: '{{ windows.default_password }}'
                        datacenter: '{{ vcenter_details.datacenter }}'
                        folderPath: '/'
                    backupName: '{{ backupName }}'
            check_mode: no
            register: resultSQLDBRestore
            failed_when: not resultSQLDBRestore.data is defined or resultSQLDBRestore.data.err_message is defined or resultSQLDBRestore.data.status == false
            changed_when: resultSQLDBRestore.data is defined and not resultSQLDBRestore.data.err_message is defined and resultSQLDBRestore.data.status == true
