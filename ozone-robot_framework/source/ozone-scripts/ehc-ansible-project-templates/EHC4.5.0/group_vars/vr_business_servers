# ------------------------------------------------------------------------------
# Copyright (C) 2016-2017 DELL EMC Corporation. All Rights Reserved.

# This software contains the intellectual property of DELL EMC Corporation
# or is licensed to DELL EMC Corporation from third parties.  Use of this
# software and the intellectual property contained therein is expressly
# limited to the terms and conditions of the License Agreement under which
# it is provided by or on behalf of DELL  EMC.

# ------------------------------------------------------------------------------

ovaType: 'vRealize Business Standards'
ovaVersion: '7.1'

ova_manifest_lookup: '{{ sw_repository_manifest.content.vrb_ova }}'
ovaSource: '{{ sw_repository_manifest.base_path }}/{{ ova_manifest_lookup.relative_path }}/{{ ova_manifest_lookup.file_name }}'

ovaConfigDict:
    network: '{{   vcenter_network   }}'
    ip: '{{ ansible_host_ip }}'
    gateway: '{{ common.management_network_gateway }}'
    netmask: '{{ common.management_network_netmask }}'
    dns: '{{ common.dns_server_ip }}'
    root_password: '{{ vrealize_business_standard.vra_business_administrator_password }}'
    ssh_enabled: 'True'
    currency: '{{ vrealize_business_standard.vra_business_currency }}'


vmDict:
    host: '{{ ansible_host }}'
    vmHost: '{{ ansible_host }}'
    username: root
    password: '{{ vrealize_business_standard.vra_business_administrator_password }}'
    licenseKey: '{{ license_key.vrealize_business_standard }}'
    vmName: '{{ vmname }}'
    vmUserName: 'root'
    vmPassword: '{{ vrealize_business_standard.vra_business_administrator_password }}'
    dcName: '{{ vcenter_datacenter }}'

vRAInitializationDict:
    host: '{{ host_address.vra_load_balancer.fqdn }}'
    username: '{{ vrealize_automation.default_tenant.username }}'
    password: '{{ vrealize_automation.default_tenant.password }}'
    tenant: 'vsphere.local'
    tenantId: "{{ hostvars['vra_primary'].firstTenant.tenantID }}"
    tenantID: "{{ hostvars['vra_primary'].firstTenant.tenantID }}"

vRADict:
    host: '{{ host_address.vra_load_balancer.fqdn }}'
    username: '{{ ad_user.ehc_tenant_admin.username }}'
    password: '{{ ad_user.ehc_tenant_admin.password }}'
    tenant: "vsphere.local"
    tenantId: "{{ hostvars['vra_primary'].firstTenant.tenantID }}"
    tenantID: "{{ hostvars['vra_primary'].firstTenant.tenantID }}"

vRACred_ehc_tenant_admin:
    host: '{{ host_address.vra_load_balancer.fqdn }}'
    username: '{{ ad_user.ehc_tenant_admin.username }}'
    password: '{{ ad_user.ehc_tenant_admin.password }}'
    tenant: "vsphere.local"
    tenantId: "{{ hostvars['vra_primary'].firstTenant.tenantID }}"
    tenantID: "{{ hostvars['vra_primary'].firstTenant.tenantID }}"

vCenterDetails:
    alias: VC-01
    url: '{{ vcenter.fqdn }}'
    userName: '{{ ad_user.app_vrb_vcenter.username }}@{{ad_domain}}'
    password: '{{ ad_user.app_vrb_vcenter.password }}'

vRBRoleAssignment:
    name: '{{ ad_user_group.ehc_tenant_admins.groupname }}'
    domain: '{{ ad_domain }}'
    roles:
      - 'business management administrator'
      - 'business management read only user'